'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _regex = require('./regex');

var _parseNumber = require('./parseNumber');

var _parseNumber2 = _interopRequireDefault(_parseNumber);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var normalizeChunk = function normalizeChunk(chunk, chunksLength) {
  if (_regex.RE_INT_OR_FLOAT.test(chunk) && (!_regex.RE_LEADING_ZERO.test(chunk) || chunksLength === 1)) {
    return (0, _parseNumber2.default)(chunk) || 0;
  }
  return chunk.replace(_regex.RE_WHITESPACES, ' ').replace(_regex.RE_LEADING_OR_TRAILING_WHITESPACES, '');
};
exports.default = normalizeChunk;