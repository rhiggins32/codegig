'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _compareValues = require('./compareValues');

var _compareValues2 = _interopRequireDefault(_compareValues);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var compareMultiple = function compareMultiple(recordA, recordB, orders) {
  var valuesA = recordA.values;
  var valuesB = recordB.values;
  var length = valuesA.length;

  var ordersLength = orders.length;
  for (var i = 0; i < length; i++) {
    var order = i < ordersLength ? orders[i] : null;
    if (order && typeof order === 'function') {
      var result = order(valuesA[i].value, valuesB[i].value);
      if (result) {
        return result;
      }
    } else {
      var _result = (0, _compareValues2.default)(valuesA[i], valuesB[i]);
      if (_result) {
        return _result * (order === 'desc' ? -1 : 1);
      }
    }
  }
  return 0;
};

exports.default = compareMultiple;